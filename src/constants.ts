export class Constants {
    JSON_COMMA: string = ','
    JSON_COLON: string = ':'

    JSON_OPEN_ARRAY: string = '['
    JSON_OPEN_OBJECT: string = '{'

    JSON_CLOSE_ARRAY: string = ']'
    JSON_CLOSE_OBJECT: string = '}'

    JSON_DOUBLE_QUOTE: string = '"'

    JSON_SPACE: string = '\s'
    JSON_TAB: string = '\t'
    JSON_NEW_LINE: string = '\n'
    JSON_CARRIAGE_RETURN: string = '\r'

    constructor() {}
}